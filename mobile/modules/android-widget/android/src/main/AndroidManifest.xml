<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <application>
    <!--
      Declares the home-screen widget provider. The launcher resolves it via
      the AppWidgetManager, so exported="false" is fine here.
    -->
    <receiver
      android:name="expo.modules.androidwidget.QuickAddWidgetProvider"
      android:exported="false"
      android:label="@string/widget_quickadd_label">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/quickadd_widget_info" />
    </receiver>
  </application>
</manifest>
